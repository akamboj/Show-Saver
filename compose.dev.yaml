services:
    show-saver:
        image: show-saver
        container_name: show-saver
        build: .
        env_file:
            - ./dev.env
        environment:
            - URL2=testy
        volumes:
            - ./.local/config:/config
            - ./.local/tvshows:/tvshows
            - ./.local/tmp:/tmp